// Use Case: Login
import '../entities/user_entity.dart';

class LoginUseCase {
  Future<UserEntity?> execute(String email, String password) async {
    // TODO: Implementar autenticación contra backend Dart Frog
    throw UnimplementedError();
  }
}
